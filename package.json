{
  "name": "cli-do",
  "version": "0.1.0",
  "description": "A beautiful task manager for the terminal",
  "module": "src/index.tsx",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.tsx",
    "build": "bun build src/index.tsx --outdir dist --target bun",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "react": "^18.2.0",
    "ink": "^4.4.1"
  },
  "engines": {
    "node": ">=18"
  }
}
